# Copy to .env and fill in. Never commit .env.

# IBKR (IB Gateway paper port is 4002, live is 4001; TWS paper 7497 / live 7496)
IB_HOST=127.0.0.1
IB_PORT=4002
IB_CLIENT_ID=1

# LLM
ANTHROPIC_API_KEY=

# Optional: where Parquet/SQLite data lives (defaults to ./data)
HT_DATA_DIR=
